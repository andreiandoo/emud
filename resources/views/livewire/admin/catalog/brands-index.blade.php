<div><x-admin.page-header title="Branduri" subtitle="Producători disponibili în editorul de produse." /><form wire:submit="save" class="mb-6 grid gap-3 rounded-xl border bg-white p-5 md:grid-cols-2"><input wire:model="name" placeholder="Nume brand" ><input wire:model="website" type="url" placeholder="Website" ><textarea wire:model="description" placeholder="Descriere" class="md:col-span-2"></textarea><input type="file" wire:model="logo" accept="image/*"><label class="text-sm"><input type="checkbox" wire:model="isActive"> Activ</label><button class="btn-primary">Salvează</button></form><div class="card">@foreach($brands as $brand)<div class="flex items-center justify-between border-t p-4"><button wire:click="edit({{ $brand->id }})" class="font-semibold hover:underline">{{ $brand->name }}</button><div class="text-sm text-stone-500">{{ $brand->products_count }} produse · {{ $brand->is_active ? 'activ' : 'inactiv' }} <button wire:click="delete({{ $brand->id }})" wire:confirm="Ștergi brandul?" class="ml-3 text-red-600">Șterge</button></div></div>@endforeach</div></div>
